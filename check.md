# 전류가 말하는 비밀

**반도체 사이드채널 공격 재현 및 방어 효과 검증**

> 공업일반 탐구 프로젝트 · 방학 내 완성 목표
> 이 문서는 CLI에서 Claude와 이어서 작업하기 위한 인수인계 문서다.

---

## 0. 한 문장 요약

AES를 실행하는 마이크로컨트롤러의 전력 소비 신호만으로 비밀키를 복구하는 AI를 만들고, 방어 기법이 적용되면 그 공격이 얼마나 무력해지는지를 통제된 조건에서 측정·시각화한다. 하드웨어 없이 공개 데이터셋만으로 전부 소프트웨어로 수행한다.

---

## 1. 내가 만들어야 하는 것 (범위 확정)

| # | 만드는 것 | 규모 | 비고 |
|---|---|---|---|
| 1 | **공격 AI 2종** (MLP, 경량 CNN) | 각 15~30줄 | 구조가 논문에 공개됨. PyTorch로 조립 |
| 2 | **학습·공격 파이프라인** | ~200줄 | 학습 루프, 키 바이트 복구, GE 계산 |
| 3 | **실험 배치 러너** | ~80줄 | 6개 조건 자동 실행 → JSON 산출 |
| 4 | **React 시각화 웹** | 컴포넌트 4~5개 | 이 프로젝트의 얼굴 |
| 5 | **탐구보고서** | 별도 문서 | 서론·이론배경은 이미 작성됨 |

**AI를 밑바닥부터 발명하는 게 아니다.** 신경망 구조는 선행연구에 공개되어 있고, 학습은 PyTorch가 처리한다. 실제로 손이 많이 가는 곳은 (2)의 키 복구·GE 계산 로직과 (4)의 시각화다.

**GPU 불필요.** 축소 데이터셋 + 경량 모델이라 노트북 CPU로 조건당 수 분이면 학습이 끝난다.

---

## 2. 배경 지식 (작업 전 반드시 이해할 것)

### 사이드채널 공격
AES는 수학적으로 안전하지만, **그것을 실행하는 물리적 칩**은 계산 중 전력을 소비한다. 이 부수적 물리 신호를 사이드채널(side-channel)이라 한다.

- CMOS 트랜지스터는 0↔1 스위칭 시 순간적으로 전류를 더 쓴다
- 스위칭 양은 처리 중인 데이터의 **1의 개수(해밍 웨이트)** 와 통계적으로 비례한다
- AES는 `S-box(평문 ⊕ 키)` 연산을 하는데, 이 중간값은 **키에 의존**한다
- 따라서 전력 패턴 → 중간값 → 키 를 역추적할 수 있다

### 공격 방식: 각개격파
AES-128 키는 16바이트지만 한 번에 다 뚫지 않는다. S-box는 `평문 1바이트 ⊕ 키 1바이트`만 쓰므로, 키 1바이트(후보 256개)씩 공략한다. 16번 반복하면 전체 키 복구.

### 용어
| 용어 | 뜻 |
|---|---|
| **파워 트레이스** | 칩의 전력 소비를 시간순으로 촘촘히 측정한 시계열 |
| **프로파일링 셋** | 모델을 **학습**시키는 트레이스 (50,000개) |
| **어택 셋** | 학습된 모델로 **공격**하는 트레이스 (10,000개) |
| **마스킹** | 중간값에 난수를 섞어 전력↔키 상관관계를 끊는 방어 |
| **비동기화(desync)** | 트레이스를 시간축으로 흔들어 정렬을 깨는 방어(하이딩 계열) |
| **GE (Guessing Entropy)** | "평균 몇 개의 트레이스를 봐야 정답 키가 1순위가 되는가". 낮을수록 공격이 강함 |
| **MCU** | 마이크로컨트롤러. CPU+메모리+입출력을 칩 하나에 담은 것 |

### 왜 서버 CPU가 아니라 MCU인가
- CPU는 2010년 **AES-NI** 하드웨어 명령어 도입으로 룩업 테이블을 없애고 상수 시간 실행이 되어, AES에 대한 캐시 타이밍 공격이 사실상 종료됨
- MCU는 **원가 문제로 전용 암호 회로를 넣을 수 없다.** 300원짜리 칩에 AES-NI를 박을 수 없음
- MCU는 캐시가 없어 캐시 타이밍은 성립 안 하지만, **테이블을 읽을 때의 전력 소비**가 샌다
- MCU는 단순해서 명령어를 순차 실행 → 노이즈가 적어 신호가 오히려 깨끗하게 드러남
- 공격자가 물리적으로 손에 넣을 수 있다 (도어락, 카드, 하드웨어 지갑) → 위협 모델이 성립

### MCU가 쓰이는 곳
자동차 1대에 100~150개, 가전, 스마트홈, 웨어러블, 산업 센서, 의료기기, 그리고 **AES 키를 품은 것들** — 신용카드·교통카드·SIM·전자여권·스마트키·도어락·하드웨어 지갑·스마트 계량기. 공통점은 전부 공격자가 물리적으로 확보 가능하다는 점.

---

## 3. 연구 포지셔닝 (중요 — 과장 금지)

### 이 프로젝트는 "새로운 발견"이 아니다
선행연구 조사 결과 아래는 **전부 이미 확립된 영역**이다:

- 딥러닝 기반 사이드채널 공격 (2016~, 매년 수십 편)
- MLP vs CNN 비동기화 비교 → **ASCAD 원논문(2019)에 이미 있음**
- 경량 CNN 방법론 → Zaid et al. (2020), 191 트레이스로 복구
- XAI로 누설 지점 찾기 → GradVis, CGV, occlusion, LeaX, ExDL-SCA
- 실시간 공격 탐지 → 2025년 다수
- 마스킹+하이딩 결합 효과 → 이미 업계 표준 관행이자 연구 완료
- 학습 트레이스 수를 줄였을 때의 성능 저하 → mixup, CGAN 등으로 연구됨

독일 BSI는 2024년 2월 **AIS 46 개정판**에서 머신러닝 기반 사이드채널 저항성 평가 지침을 공식 발표했다. 국가 인증기관이 시험 절차를 만들 정도로 확립된 위협이다.

### 따라서 이 프로젝트의 정체성

> **재현 검증 (Replication Study)**
> "국제 인증 표준까지 존재하는 확립된 위협을, 공개 데이터로 직접 재현하고 방어 기법의 효과를 정량 검증한다."

발표 시 **절대 "세계 최초로 발견"이라고 말하지 말 것.** 재현은 과학에서 독립적으로 가치 있는 활동이며, Wouters et al.(2020)이 Zaid 논문을 재검증한 선례가 있다.

### 차별 요소 (정직한 버전)
1. **탐구 과정의 기록** — 차별점을 찾아 선행연구를 반복 조사했고, 이미 다 되어 있음을 확인해 재현 검증으로 방향을 정한 과정 자체를 보고서에 서술. (이 §3이 그 증거)
2. **공업일반 산업표준 맥락** — 제품의 표준화·품질 관리 단원과 직결
3. **비전공자용 시각화** — 기존 연구는 전부 전문가용 논문. 결과를 체험 가능하게 만든 도구는 드묾

---

## 4. 실험 설계

### 통제 비교: 2 모델 × 3 방어 강도 = 6 조건

|  | desync 0 | desync 50 | desync 100 |
|---|---|---|---|
| **MLP** | 조건 1 | 조건 2 | 조건 3 |
| **CNN (Zaid식 경량)** | 조건 4 | 조건 5 | 조건 6 |

**왜 이 설계인가**
ASCAD는 **동일 기기·동일 트레이스**에 인위적 지터만 다르게 적용한 desync 0/50/100 버전을 함께 제공한다. → "기기가 달라서 그런 거 아니냐"는 반박이 원천 차단됨.

이론적 예측: MLP는 동기화 상태에서 CNN과 대등하나 지터가 커지면 무너진다. CNN은 합성곱의 이동 등변성 덕분에 버틴다. **이 예측을 직접 재현해 그래프로 증명하는 것이 핵심 결과물.**

### 측정 지표
- **주 지표: GE** — 트레이스 개수에 따른 정답 키 순위 곡선
- 보조: 학습 시간, 파라미터 수 → "공격이 가능하다" vs "공격이 현실적이다" 구분

---

## 5. 데이터셋

### ASCAD (ATMega8515, 고정키)

- 제작: **ANSSI** (프랑스 국가정보시스템보안청)
- 대상 칩: **ATmega8515** — 8비트 AVR 마이크로컨트롤러 (아두이노의 ATmega328P와 같은 계열)
- 대상 연산: 1차 부울 마스킹이 적용된 소프트웨어 AES-128
- 측정: 전자파(EM) 트레이스
- 구성: profiling 50,000 + attack 10,000, 각 **700 샘플** (원본 raw에서 누설 구간만 추출)

### 왜 이 데이터셋인가
1. DL-SCA의 표준 벤치마크 → 결과를 기존 논문과 비교 가능
2. 700샘플로 축소되어 **노트북 CPU로 학습 가능** (원본 raw는 100,000 샘플, 71GB)
3. desync 변형본을 공식 제공 → 통제 비교의 전제
4. 축소판은 "이제 공격하기 쉬워졌다"는 평가가 있음 → 짧은 시간에 재현 성공 가능하므로 우리에겐 유리

### 다운로드

**공식 저장소**: https://github.com/ANSSI-FR/ASCAD
**고정키 안내 페이지**: https://github.com/ANSSI-FR/ASCAD/blob/master/ATMEGA_AES_v1/ATM_AES_v1_fixed_key/Readme.md

```bash
git clone https://github.com/ANSSI-FR/ASCAD.git
cd ASCAD/ATMEGA_AES_v1/ATM_AES_v1_fixed_key/
wget https://www.data.gouv.fr/api/1/datasets/r/e7ab6f9e-79bf-431f-a5ed-faf0ebe9b08e -O ASCAD_data.zip
unzip ASCAD_data.zip
```

- 다운로드 약 **4.2GB**, 압축 해제 후 약 **7.3GB**
- 압축 안에 아래가 모두 포함됨:
  - `ASCAD_data/ASCAD_databases/ASCAD.h5` — desync 0 (원본 추출)
  - `ASCAD_data/ASCAD_databases/ASCAD_desync50.h5` — 최대 50샘플 지터
  - `ASCAD_data/ASCAD_databases/ASCAD_desync100.h5` — 최대 100샘플 지터
  - `ATMega8515_raw_traces.h5` — 원본 raw (용량 큼, 이번 프로젝트에선 불필요)

> 네트워크 좋은 환경에서 미리 받을 것. 저장소에 SHA-256 체크섬이 공개되어 있으니 검증 가능.

### 참고: 저장소에 이미 들어 있는 유용한 자산
- `ASCAD_generate.py` — desync 파라미터를 바꿔 데이터셋을 재생성하는 스크립트. profiling 0~50000, attack 50000~60000, target_points 45400~46100(700점) 설정이 여기 있음
- `ASCAD_test_models.py` — 학습된 모델로 **바이트별 정답 키 순위**를 출력. 우리 GE 계산 로직의 참고 구현
- 사전 학습된 모델 파일도 포함 → 파이프라인 검증용 대조군으로 활용 가능

---

## 6. 기술 스택

### 결정: Python(계산) + React(시각화), 런타임 분리

```
[Python] 실험 배치 실행 → results/*.json → [React] 정적 로드 → 시각화
```

**둘이 런타임에 통신하지 않는다.** API 서버 불필요.
- 실험은 오프라인 배치로 1회 실행 → JSON 산출
- React는 그 JSON만 읽어 렌더 → 정적 사이트 배포 가능, 발표 시 안정적
- "Python + React 궁합" 문제가 발생할 여지 자체가 없음

| 대안 | 판단 |
|---|---|
| Streamlit (순수 Python) | 빠르지만 시각화 자유도 낮음. 시각화가 초점이면 부적합 |
| Flask API + React | 실시간 추론이 필요 없으므로 과설계 |
| **Python 배치 + React 정적** | ✅ 채택 |

### 라이브러리
- **Python**: numpy, h5py, torch, matplotlib(디버그용), tqdm
- **React**: Vite + TypeScript, Recharts, Tailwind

---

## 7. 디렉토리 구조

```
sca-project/
├── README.md
├── python/
│   ├── requirements.txt
│   ├── src/
│   │   ├── data.py            # ASCAD HDF5 로더
│   │   ├── models.py          # MLP, Zaid식 경량 CNN
│   │   ├── train.py           # 학습 루프
│   │   ├── attack.py          # 키 바이트 복구, GE 계산
│   │   └── run_experiments.py # 6개 조건 배치 → JSON 출력
│   └── data/                  # ASCAD .h5 (gitignore)
├── results/                   # Python → React 계약 지점
│   ├── ge_curves.json
│   ├── key_recovery.json
│   └── traces_sample.json
└── web/
    ├── package.json
    └── src/
        ├── App.tsx
        ├── sim/                     # 시뮬레이션 모드 (브라우저 계산)
        └── components/
            ├── ChipTraceView.tsx    # 칩 동작 + 파형 동기화
            ├── KeyRecoveryGrid.tsx  # 16바이트 추리 패널
            ├── DefenseToggle.tsx    # desync / 모델 전환
            └── GEChart.tsx          # GE 곡선 비교
```

---

## 8. 환경 세팅

```bash
cd python
python3 -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate
pip install --upgrade pip
pip install numpy h5py torch matplotlib tqdm
pip freeze > requirements.txt
```

```bash
cd web
npm create vite@latest . -- --template react-ts
npm install
npm install recharts
```

---

## 9. 작업 순서

- [ ] **1. 데이터 구조 확인** — `data.py`로 HDF5 열어 profiling/attack shape 출력, 트레이스 1개 플롯
- [ ] **2. 모델 정의** — `models.py`에 MLP와 Zaid식 CNN (얕은 구조, 짧은 필터, 배치정규화, 원사이클 LR)
- [ ] **3. 학습 파이프라인** — `train.py`, 우선 desync 0 + MLP 한 조건만 돌려 정상 동작 확인
- [ ] **4. 공격 및 GE 구현** — `attack.py`, 키 1바이트 복구 성공시키기. **첫 번째 관문**
- [ ] **5. 6개 조건 배치 실행** — `run_experiments.py`
- [ ] **6. 시각화용 JSON 산출** — §11 계약대로 3개 파일
- [ ] **7. React 시각화** — §10 설계 순서대로. 재생 시각 `t` 상태를 먼저 잡고 패널을 붙일 것
- [ ] **8. 시뮬레이션 모드** — 브라우저에서 해밍 웨이트+노이즈로 트레이스 생성, 사용자 키 입력 지원
- [ ] **9. 보고서 통합** — 서론/이론배경 문서에 실험 결과 삽입

---

## 10. 화면 설계 (시각화)

### 설계 원칙
1. **동기화가 핵심** — 칩 동작 / 전력 파형 / 키 추리를 따로 보여주면 인과관계가 안 보인다. 같은 시간축에 묶어 함께 움직여야 한다.
2. **"AI가 키를 알아내는 순간"이 클라이맥스** — 네트워크 다이어그램류는 임팩트 없음.
3. **모든 렌더는 사전 계산 결과의 재생** — React는 추론하지 않는다.

### 상태 구조 (이게 전부다)

```
사용자 조작                공유 상태              화면 패널
─────────────            ──────────           ──────────────
재생 제어      ┐                          ┌→ 칩 + 파형
트레이스 수    ├──→  { t, conditionId }  ──┼→ 키 추리 그리드
방어 강도      │                          └→ GE 곡선
모델 선택      ┘
```

**컨트롤이 패널을 직접 건드리지 않는다.** 모든 조작은 최상위 상태 하나만 갱신하고, 패널 셋은 그 상태를 구독만 한다. 동기화 코드를 따로 짤 필요가 없다.

- `t` 변경 → JSON 배열 인덱스만 교체 (시나리오 1)
- `conditionId` 변경 → 다른 조건 데이터로 전환 (시나리오 2·3)
- 둘 다 재계산이 없어 반응이 즉각적

### 클릭 → 반응 시나리오

| 사용자 조작 | 화면 반응 |
|---|---|
| 슬라이더를 오른쪽으로 (트레이스 수 증가) | 16개 타일 순위가 256위→1위로 하강, 키 바이트가 하나씩 확정 |
| 방어 강도 100 선택 | 파형 정렬이 깨져 흔들리고, 순위가 수렴하지 못함 |
| 모델을 MLP로 전환 (방어 고정) | CNN보다 먼저 무너지는 대비, GE 곡선 두 개가 갈라짐 |

이 세 시나리오가 발표 서사 그대로다: **뚫린다 → 막힌다 → 막는 것도 상대에 따라 다르다.**

### 두 가지 모드

| | 시뮬레이션 모드 | 실측 모드 |
|---|---|---|
| 키 | **사용자가 직접 입력 가능** | ASCAD 고정키 |
| 트레이스 | 해밍 웨이트 모델 + 가우시안 노이즈로 즉석 생성 | 실제 칩에서 측정된 기록 |
| 목적 | 원리 체감, 즉각 반응 | 실제 실험 결과 |
| 표기 | **"합성 데이터"임을 화면에 명시 필수** | "ATmega8515 실측" 명시 |

> ASCAD 트레이스는 실제 칩에서 물리적으로 측정된 기록물이다. 사용자가 임의의 키를 넣으면 그에 대응하는 실측 트레이스는 존재하지 않는다. 따라서 키 입력은 시뮬레이션 모드에서만 제공하고, 합성 데이터임을 반드시 표기한다. **이 구분을 흐리면 프로젝트의 정직성이 무너진다.**

### 화면 구성 (스크롤 순서)

**① 도입 — 위협 실감시키기**
"지금 여러분 지갑 속 카드와 현관 도어락 안에 이 칩이 들어 있습니다" / MCU가 쓰이는 곳 / 왜 서버가 아니라 MCU인가

**② 칩 동작 + 전력 파형 (동기화 뷰) — 핵심 화면**
- 좌: MCU 모식도, S-box 룩업 지점이 순차 점등
- 우: 같은 시각의 파형이 좌→우로 그려짐
- 재생/정지/속도 컨트롤, 봉우리 호버 시 "이 순간 S-box[평문⊕키]를 읽는 중" 툴팁
- 강조: 봉우리 높이 ∝ 해밍 웨이트

**③ 키 추리 패널 — 클라이맥스**
- 16개 바이트 타일을 4×4 배치
- 각 타일에 정답 키의 현재 순위(1~256), 낮아질수록 진한 색
- 1위 도달 시 확정 상태 전환 + 실제 키 바이트 값 노출
- 하단에 복구된 키 진행 바
- 옆에 후보 256개 막대그래프(1바이트 기준)로 정답이 솟아오르는 모습

**④ 방어 토글 — 반전 구간**
desync 0/50/100 세그먼트 + MLP/CNN 전환. ②의 파형이 실제로 흔들리고 ③이 수렴하지 못하는 모습

**⑤ GE 곡선 비교 차트**
x=어택 트레이스 수, y=GE. 6개 조건 겹쳐 그리기, 범례 토글, y=1 기준선

**⑥ 비용 요약 표**
조건별 파라미터 수 / 학습 시간 / GE 1 도달 트레이스 수

**⑦ 한계와 정직한 서술**
재현 검증이며 새 공격법 아님 / 공개 데이터셋 사용, 직접 측정 안 함 / 시뮬레이션 모드는 합성 데이터

### 구현 메모
- 차트: Recharts. 파형·칩 모식도: 인라인 SVG + `requestAnimationFrame`
- 최상위 상태 `t` 하나를 ②③⑤가 구독 → 동기화가 자연히 성립
- 애니메이션은 `transform`/`opacity`만, `prefers-reduced-motion` 존중
- 시뮬레이션 모드는 브라우저에서 계산 가능 (해밍 웨이트 + 노이즈 + 상관계수). Python 불필요

---

## 11. JSON 계약 (Python ↔ React)

**`results/ge_curves.json`** — ⑤⑥ 차트용
```json
{
  "meta": { "dataset": "ASCADf", "target_byte": 2, "trace_step": 10 },
  "conditions": [
    {
      "id": "mlp_desync0",
      "model": "MLP",
      "desync": 0,
      "params": 123456,
      "train_seconds": 84.2,
      "ge_curve": [256, 200, 140, 88, 41, 12, 3, 1, 1, 1],
      "traces_to_ge1": 412
    }
  ]
}
```
> `ge_curve[i]` = 어택 트레이스 `i * trace_step`개 사용 시 정답 키의 평균 순위

**`results/key_recovery.json`** — ③ 키 추리 패널용
```json
{
  "condition_id": "cnn_desync0",
  "true_key": [77, 251, 224, 242, 114, 33, 254, 16, 167, 141, 74, 220, 142, 73, 4, 105],
  "trace_step": 10,
  "byte_ranks": [
    [256, 180, 92, 31, 7, 1, 1],
    [256, 210, 140, 66, 22, 4, 1]
  ],
  "candidate_scores_byte2": [
    [0.004, 0.003, 0.005, "... 256개"]
  ]
}
```
> `byte_ranks[b][i]` = 바이트 b의 정답 키 순위. 16개 타일 애니메이션의 원천
> `candidate_scores_byte2`는 막대그래프용. 용량이 크므로 1바이트만, 스텝 성기게

**`results/traces_sample.json`** — ② 파형 뷰용
```json
{
  "desync0":   [[0.12, 0.18, "... 700 샘플"]],
  "desync50":  [[0.11, 0.19, "... 700 샘플"]],
  "desync100": [[0.13, 0.17, "... 700 샘플"]],
  "poi_ranges": { "sbox_lookup": [140, 220] }
}
```
> 방어 강도별 5~10개만. `poi_ranges`는 S-box 연산 구간 표시용(SNR 분석으로 산출)

---

## 12. 주의사항

- **과장 금지.** "새 공격법 발명" ❌ / "확립된 위협의 재현 검증" ⭕
- 재현이 목적이므로 **시드 고정과 실행 조건 기록**이 필수. 재현 불가능한 결과는 이 프로젝트의 목적과 모순
- 논문 수치(예: Zaid의 191 트레이스)와 다른 값이 나와도 괜찮다. **불일치 자체가 보고할 가치가 있는 결과**
- GE는 여러 번 반복(예: 100회) 후 평균내야 안정적인 곡선이 나옴
- 시뮬레이션 모드와 실측 모드를 화면에서 명확히 구분할 것

---

## 13. 참고문헌

- Kocher, Jaffe, Jun (1999). *Differential Power Analysis*. CRYPTO '99. https://doi.org/10.1007/3-540-48405-1_25
- Benadjila et al. (2020). *Deep learning for side-channel analysis and introduction to ASCAD database*. JCEN 10, 163-188. https://link.springer.com/article/10.1007/s13389-019-00220-8
- ASCAD 저장소: https://github.com/ANSSI-FR/ASCAD
- ASCAD 고정키 안내: https://github.com/ANSSI-FR/ASCAD/blob/master/ATMEGA_AES_v1/ATM_AES_v1_fixed_key/Readme.md
- Zaid et al. (2020). *Methodology for Efficient CNN Architectures in Profiling Attacks*. TCHES 2020(1).
- Picek et al. *SoK: Deep Learning-based Physical Side-channel Analysis*. https://dl.acm.org/doi/10.1145/3569577
- scadl (Ledger-Donjon): https://github.com/Ledger-Donjon/scadl
- NinjaLab (2024). *EUCLEAK*. https://ninjalab.io/eucleak/
- ANSSI 보호된 AES 구현: https://github.com/ANSSI-FR/secAES-ATmega8515